# SentinelID
SentinelID is a security-focused authentication system that replaces passwords with a passkey-style face login, built as a desktop app with a companion web dashboard. It performs on-device computer vision to verify identity, prove live presence, and detect spoofing attempts—without storing or transmitting raw face images by default.
