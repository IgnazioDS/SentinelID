openapi: 3.0.0
info:
  title: SentinelID Edge API
  version: 0.1.0
paths:
  /v1/health:
    get:
      summary: Health Check
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
